# Deny everything by default; this folder holds a lot of unrelated personal/
# project files (Word docs, pptx, other data extracts) that should stay local.
# Only the dashboard code + the two pilot data files it reads are tracked.
*

!.gitignore
!README.md
!requirements.txt

!Stock_availability.py
!theme.py
!analyze_pilot_stock.py

!pages/
!pages/*.py

!.streamlit/
!.streamlit/config.toml

!Pilot_stcok availability.xlsx
!Pilot_prescription.xlsx
